# Union Rochefortoise — Base de données joueurs

## Agent skills

### Issue tracker

Les issues et PRDs vivent dans GitHub Issues de ce dépôt (`David-coachadjoint/UR---Players`). Voir `docs/agents/issue-tracker.md`.

### Triage labels

Labels par défaut : `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. Voir `docs/agents/triage-labels.md`.

### Domain docs

Contexte unique — un seul `CONTEXT.md` + `docs/adr/` à la racine. Voir `docs/agents/domain.md`.
